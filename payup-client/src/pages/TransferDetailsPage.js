import React from "react";

const TransferDetaiPage = ({ transfer }) => {

    return (
        <div>
            This is the transfer detail page for transfer #{transfer.transferId}
        </div>
    );
};

export default TransferDetaiPage;